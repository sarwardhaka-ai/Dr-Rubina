name: SEO & Best Practices Check

on:
  push:
    branches: [ main ]

jobs:
  webhint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install webhint
        run: npm install -g hint

      - name: Run SEO checks
        run: npx hint public --format summary
